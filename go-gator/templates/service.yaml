apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-gator.fullname" . }}
  labels:
    {{- include "go-gator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-gator.selectorLabels" . | nindent 4 }}
