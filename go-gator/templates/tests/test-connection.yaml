apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-gator.fullname" . }}-test-connection"
  labels:
    {{- include "go-gator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-gator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
