---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: feeds.aggregator.teamdev.com
spec:
  group: aggregator.teamdev.com
  names:
    kind: Feed
    listKind: FeedList
    plural: feeds
    singular: feed
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Feed is the Schema for the feeds API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FeedSpec defines the desired state of Feed
            properties:
              foo:
                description: Foo is an example field of Feed. Edit feed_types.go to
                  remove/update
                type: string
            type: object
          status:
            description: FeedStatus defines the observed state of Feed
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
